name: Maintenance Service CD

on:
  workflow_run:
    workflows: ["Maintenance Service CI"]
    types: [completed]
    branches: [develop, main]

concurrency:
  group: maintenance-service-cd-${{ github.event.workflow_run.head_branch }}
  cancel-in-progress: false

jobs:
  cd-dev:
    name: CD — Dev
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push' && github.event.workflow_run.head_branch == 'develop' }}
    uses: FleetOps-V2/fleetops-github-workflows/.github/workflows/cd-ecr.yml@main
    with:
      service-name: maintenance-service
      ci-workflow-name: Maintenance Service CI
      deploy-env: dev
      head-sha: ${{ github.event.workflow_run.head_sha }}
      health-url: "https://fleetops.website/api/tasks"
    secrets: inherit

  cd-prod:
    name: CD — Prod
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push' && github.event.workflow_run.head_branch == 'main' }}
    uses: FleetOps-V2/fleetops-github-workflows/.github/workflows/cd-ecr.yml@main
    with:
      service-name: maintenance-service
      ci-workflow-name: Maintenance Service CI
      deploy-env: prod
      head-sha: ${{ github.event.workflow_run.head_sha }}
      health-url: "https://fleetops.website/api/tasks"
    secrets: inherit
